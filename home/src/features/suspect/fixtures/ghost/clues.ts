import { ClueType } from "@/features/suspect/types";
import { createAdditionalQuestions } from "../utils";

export const ghostAdditionalQuestions = createAdditionalQuestions([]);

// TODO: Replace with the final haunted-house clue placement data.
export const ghostClues: ClueType[] = [];
