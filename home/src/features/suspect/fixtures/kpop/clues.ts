import { ClueType } from "@/features/suspect/types";
import { createAdditionalQuestions } from "../utils";

export const kpopAdditionalQuestions = createAdditionalQuestions([
  {
    question: "악령이 누구인가요?",
    answer: "석PD",
  },
]);

export const kpopClues: ClueType[] = [];
